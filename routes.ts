export const ROUTES = {
  AUTH: {
    LOGIN: "/auth/login",
    VERIFY_REQUEST: "/api/auth/verify-request",
  },
};

export const authRoutes = ["/auth/login", "/api/auth/verify-request"];

export const publicRoutes = ["/"];

export const apiAuthPrefix = "/api";

export const DEFAULT_LOGIN_REDIRECT = "/";
